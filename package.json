{
  "name": "pura",
  "version": "0.0.1",
  "description": "functional utilities collection",
  "scripts": {
    "compile": "rm -rf ./dist && tsc -p ./ts-config.json",
  },
  "author": "Rafe Lepre",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^2.7.1"
  }
}